// Placeholder -- generate/store a keypair in the hardware keychain; encrypt/decrypt message
// payloads so mesh relays only ever see opaque ciphertext. Keys never leave hardware.
// Needs react-native-keychain (removed in the blind-nav pivot; re-add for messaging).
export function useSecureVault() {
  return {};
}
